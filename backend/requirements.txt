# =============================================================================
# SafeRAG 后端依赖 —— 按 TPU 盒子实测版本锁定（Python 3.10.12, 2026-08-25）
#
# 分组说明:
#   ① Web 后端框架
#   ② RAG / 文档处理 / 检索
#
# 推理引擎（Qwen3.5 TPU 服务）已拆到独立仓库 saferag-infer，依赖不再混在本表；
# 本后端只通过 HTTP 调用引擎（QWEN_DOC_URL/QWEN_CHAT_URL）。
#
# 安装: pip install -r backend/requirements.txt
# =============================================================================

# --- ① Web / API ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
pydantic==2.13.4
pydantic_core==2.46.4
python-multipart==0.0.32
pytest==9.1.1
PyJWT==2.10.1

# --- ② RAG / 文档处理 ---
onnxruntime==1.23.2
chromadb==1.5.9
jieba==0.42.1
rank_bm25==0.2.2
python-docx==1.2.0
pypdf==6.16.2
tokenizers==0.22.2
# numpy 被 embedding/reranker 直接 import（embedding_client.py / reranker.py），
# 拆引擎设置独立版本，避免沦为 chromadb/onnxruntime 的传递依赖浮版本
numpy==2.2.6